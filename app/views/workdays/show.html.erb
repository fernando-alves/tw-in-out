<p id="notice"><%= notice %></p>

<p>
  <b>Day:</b>
  <%= @workday.day.to_s(:day) %>
</p>
<%= render :partial => "punches/list", :locals => { :punches => @punches } %>

<%= form_for(@workday, url: lunch_times_path, method: :post) do |f| %>
  <%= f.hidden_field :id %>
  <%= f.submit "Add automatic lunch time" %>
<% end %>

<%= link_to 'Back', workdays_path %>
